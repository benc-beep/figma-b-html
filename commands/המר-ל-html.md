---
description: Convert Figma screens to standalone, pixel-measured HTML
argument-hint: "[figma link(s)] — or paste them in the next message"
---

Invoke the `figma-b-html` skill and follow it from the top.

Arguments, if any: $ARGUMENTS

If no Figma link came with the command, ask for one before doing anything else.
Do not guess which screen is meant from earlier in the conversation — the
command is explicit, so the target should be too.
